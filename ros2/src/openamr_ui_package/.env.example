# Copy this file to .env (same directory) and fill in real values.
# .env is gitignored and is only read by ui.launch.py -> new_ui_launch.py,
# which injects these vars into the flask_app node's process only.

ANTHROPIC_API_KEY=your-anthropic-key-here
