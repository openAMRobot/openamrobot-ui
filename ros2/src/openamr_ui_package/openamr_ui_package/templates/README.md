# Templates

Reserved for server-rendered templates if they are needed later.

The current UI is a React single-page app served from `static/app/`, so normal
frontend changes should be made in the repository-level `web/` directory.
